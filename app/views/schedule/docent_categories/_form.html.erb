<%= form_for(@schedule_docent_category) do |f| %>
  <% if @schedule_docent_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedule_docent_category.errors.count, "error") %> prohibited this schedule_docent_category from being saved:</h2>

      <ul>
      <% @schedule_docent_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :short_name %><br>
    <%= f.text_field :short_name %>
  </div>
  <div class="field">
    <%= f.label :full_name %><br>
    <%= f.text_field :full_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
