<%= form_for(@schedule_docent_week) do |f| %>
  <% if @schedule_docent_week.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedule_docent_week.errors.count, "error") %> prohibited this schedule_docent_week from being saved:</h2>

      <ul>
      <% @schedule_docent_week.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="field">
    <%= f.label :first_day %><br>
    <%= f.date_select :first_day %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
