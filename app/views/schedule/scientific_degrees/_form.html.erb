<%= form_for(@schedule_scientific_degree) do |f| %>
  <% if @schedule_scientific_degree.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedule_scientific_degree.errors.count, "error") %> prohibited this schedule_scientific_degree from being saved:</h2>

      <ul>
      <% @schedule_scientific_degree.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :short_name %><br>
    <%= f.text_field :short_name %>
  </div>
  <div class="field">
    <%= f.label :full_name %><br>
    <%= f.text_field :full_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
