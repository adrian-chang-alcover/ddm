<%= form_for(@schedule_turn_type) do |f| %>
  <% if @schedule_turn_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedule_turn_type.errors.count, "error") %> prohibited this schedule_turn_type from being saved:</h2>

      <ul>
      <% @schedule_turn_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :short_name %><br>
    <%= f.text_field :short_name %>
  </div>
  <div class="field">
    <%= f.label :full_name %><br>
    <%= f.text_field :full_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
