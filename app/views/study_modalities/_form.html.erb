<%= form_for(@study_modality) do |f| %>
  <% if @study_modality.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@study_modality.errors.count, "error") %> prohibited this study_modality from being saved:</h2>

      <ul>
      <% @study_modality.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :short_name %><br>
    <%= f.text_field :short_name %>
  </div>
  <div class="field">
    <%= f.label :full_name %><br>
    <%= f.text_field :full_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
