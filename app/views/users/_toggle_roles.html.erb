<% Role.where('priority > ?', current_user.priority).each do |role| %>
    <button href="#" class="btn btn-success" style="margin-top: 3px;">
      <i class="icon-plus" style="margin-right: 7px;"></i><%= role.name %>
    </button>
<% end %>
